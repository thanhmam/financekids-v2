The gold "xu" currency token — drop inline beside any balance or reward amount.

```jsx
<XuCoin size={20} /> <b>1.250</b>
```

Just a sized prop. Use 20px inline, 28–40px for emphasis. For a header balance chip, prefer `<StatPill kind="xu" />`.
