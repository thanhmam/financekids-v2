The signature chunky 3D button — use for every primary action and CTA.

```jsx
<Button variant="primary" size="lg" block>HỌC TIẾP</Button>
<Button variant="streak">GIỮ STREAK</Button>
<Button variant="secondary">120 xu</Button>
<Button variant="ghost">Bỏ qua</Button>
```

Variants: `primary` (green), `streak` (orange), `gold`, `white` (on a color background), `secondary` (outline), `ghost` (text). Sizes `sm`/`md`/`lg`; `block` for full width. Press sinks the button into its solid shadow. Labels are usually UPPERCASE Baloo 2.
